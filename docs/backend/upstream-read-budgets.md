---
title: 上游读取预算
description: 存储、同步、工作流 Agent、模型探测及生成协议的总请求数、累计字节与总体时间限制
---

# 上游读取预算

容量统计、提示词同步、工作流 Agent、模型探测和生成协议适配器都会读取上游成功响应。除单请求超时和 SSRF 安全传输外，服务端还对多请求操作设置共享预算；重定向、鉴权重试、递归目录、分页和任务轮询都消耗对应操作的同一份额度。

| 操作 | 总请求数 | 累计响应体 | 总体 deadline |
| --- | ---: | ---: | ---: |
| 单个 S3/R2 或 WebDAV 容量统计 | 256 | 64 MiB | 2 分钟 |
| 单分类提示词同步 | 32 | 32 MiB | 2 分钟 |
| 全部远程提示词分类同步 | 32（全部分类共享） | 32 MiB（全部分类共享） | 2 分钟 |
| 工作流 Agent 草稿 | 1 | 8 MiB | 2 分钟 |
| Gemini / OpenAI 兼容模型列表探测 | 32 | 32 MiB；单响应 8 MiB | 1 分钟 |
| 后台单模型连通测试 | 1 | 16 MiB | 30 秒客户端超时 |
| KIE / APIMart 同步图片任务轮询 | 300 | 32 MiB；单响应 512 KiB | 10 分钟 |
| 持久化视频任务轮询 | 360 | 32 MiB；单响应 1 MiB | 自任务创建起 30 分钟 |
| 火山方舟 Seedance 排队任务取消 | 1 | 64 KiB | 10 秒 |
| RunningHub 任务取消 | 1 | 512 KiB | 20 秒 |
| 画布图片或音频任务 | 1 | 32 MiB | 5 分钟 |
| KIE / APIMart 结构化生成响应 | 1 | 8 MiB | 渠道客户端超时 |
| KIE / APIMart 文件上传响应 | 1 | 512 KiB | 渠道客户端超时 |
| MiMo TTS JSON / Base64 响应 | 1 | 128 MiB | 渠道客户端超时 |
| Gemini 视频内容及通用 AI 媒体响应 | 1 | 512 MiB | 请求取消或渠道客户端超时 |

## 执行规则

- 预算从操作开始时计时，并继承 HTTP 请求取消信号；客户端断开后不继续读取上游。
- 响应体读取层累计字节，已知 `Content-Length` 超过剩余额度时直接关闭响应。
- 达到请求数、累计字节或总体时间限制后立即停止后续递归、分页或文件拉取，并返回可安全展示的错误。
- WebDAV 容量统计使用迭代目录队列，拒绝空名称、父目录和带路径分隔符的子项，避免深递归栈和目录逃逸。
- S3 容量统计拒绝重复 continuation token、负对象大小和总容量整数溢出。
- 提示词解析完成后会再次检查 deadline；预算已耗尽时不会替换当前分类数据。全量同步中先前已完整完成的分类保持不变。
- Gemini 模型分页拒绝重复 `nextPageToken`，避免上游循环分页持续消耗请求。
- KIE / APIMart 的同步图片轮询在同一个前端请求内共享请求数、字节和 deadline；每个响应采用 `limit + 1` 探测，不再把超限 JSON 静默截断后继续解析。
- 后台持久化视频任务把累计 `poll_count` 和 `response_bytes` 写入 `video_tasks`，进程重启后预算不会清零；单次轮询请求使用任务绝对 deadline。
- 火山方舟 Seedance 只在本地最后状态仍为 `queued` 时调用官方取消端点；RunningHub ComfyUI 任务使用独立官方取消接口。上游失败或不支持取消时仍完成本地取消，且不会为其他供应商猜测取消路径。
- 画布图片和音频任务在代理写入内存 recorder 时即执行 32 MiB 上限，并继承可取消的 5 分钟任务上下文。
- 结构化生成、模型测试、文件上传响应和 MiMo TTS 都在 JSON 解析或 Base64 解码前完成限量读取。Gemini 视频内容和通用 AI 媒体采用流式上限，不把完整媒体载入内存。

请求体、对象存储文件流和图片代理仍使用各自独立的体积与内容类型限制；本表仅描述上游成功响应读取预算。
