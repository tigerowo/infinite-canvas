---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- 本机订阅代理完成真实验收后，再决定是否正式纳入 Mac helper 发布流程；当前 CLIProxyAPI 仍是仓库外的实验依赖，其二进制来源、固定版本、OAuth 登录、独立 LaunchAgent、健康检查、升级和卸载由用户手动管理。`gpt-5.5`、`gemini-3.1-flash-lite` 最小文本调用与 `gpt-image-2` 图片生成/R2 交付已通过，仍需确认非方形图片方向映射，`gemini-3.1-flash-image` 需等待 Google 图片额度恢复。后续若保留该路径，需要补供应链校验、正式进程托管和订阅额度语义说明；在此之前不得宣称 ChatGPT、Antigravity、Codex 开发或图片能力拥有相互独立的免费额度池。
- 使用画布图片节点新增的四段耗时数据对比同渠道在生图工作台与无限画布的真实调用：若主要耗时在上下文准备，优先并行解析首尾帧和引用素材；若主要耗时在上游等待，继续检查 CLI helper、代理出口和供应商排队；若主要耗时在结果落位，再针对节点持久化和大画布渲染优化。没有真实数据前不调整轮询频率或增加重复请求。
- 受控 CLI completion 后续如能提供稳定的增量事件协议，再为画布快速对话接入真实流式输出；当前 helper 只提供任务状态轮询与最终文本，界面不得用合成文字或百分比冒充上游流式进度。
- 使用正式 Apple Developer ID 和公证配置构建、安装并验收 Mac CLI helper 发布包；个人自用的隔离开发安装模式已完成 Codex 与 Antigravity CLI 的真实最小调用。即梦官方 CLI 的安装、OAuth、只读账户结构、公开图片/视频模型目录和受控 adapter 已完成；会员账号已识别 `maestro` 等级，`jimeng-image-3.0` 最低规格单图与 `jimeng-video-seedance2.5` 480p/4 秒视频已完成逐次确认的真实调用，其余标记为“待真实验证”的模型仍需按最低成本逐个确认。正式分发签名、公证及参考图/视频协议继续留待后续。
- 完成 Codex 订阅应急生图结果接入：当前画布通过 `codex-cli-default` 已真实生成图片，但受控文本任务只返回 `~/.codex/generated_images/` 下的本地文件路径，尚未把图片写入素材库或 R2，也未自动创建画布图片节点。后续只能由 helper 校验固定目录、普通文件、图片 MIME、大小和新鲜度后复制结果，再沿用现有素材持久化链路；界面需明确标识其消耗 Codex 订阅额度，保持逐次确认且不得自动回退。
- 即梦积分消费明细：账户概览已预留明细区域；后续需确认官方是否提供稳定的明细接口，或在每次受控调用前后记录积分快照并形成可核对的本地账本。本阶段不采集、不推算历史消费，也不修改数据库。
- 继续跟进异步视频供应商的正式远端取消协议；火山方舟 Seedance、RunningHub ComfyUI 和 MiniMax H3 排队任务已接入各自官方取消端点，Gemini Veo、KIE、APIMart、OpenAI/grok2api、CogVideoX/Agnes、其他 MiniMax 协议和通用 HTTP 尚无可确认的运行中取消契约，当前仍只停止本地轮询和网络读取。
- 逐步清理前端 ESLint 兼容基线警告，并继续扩展浏览器端测试；当前已有正式 lint、typecheck、Vitest 脚本、任务取消服务契约测试、连接中心 React 组件状态测试，以及覆盖基础状态、API 抽屉、迁移预览、生图/视频/画布模型来源、连接测试、编辑、复制和删除的 Playwright Mock 回归。
- 正式多实例部署前配置持久受控 Redis、独立前缀、容量与可用性监控、告警和运维故障演练；本机临时 Redis 双后端运行时验收已通过，不替代生产环境验证。
- 如需与 Cloudflare 账单做精确核对，为 R2 增加独立的只读 Analytics Token 配置和 GraphQL Analytics 对账；当前月度 A/B 明细只统计 Infinite Canvas 后端发起的操作，优先用于个人本机写入保护。
- 全面优化管理后台 UI：当前设置、用户、素材、提示词、日志等后台页面信息密度偏高、视觉层级不清晰，长表单与多类配置集中展示，整体较杂乱。后续先盘点后台信息架构并补齐 STITCH 桌面、窄屏、深色与浅色设计稿及设计 token；视觉层面重点重做主标题与页面说明、侧边栏层级和选中态、主次按钮与危险操作、表单控件、卡片、表格、抽屉、状态反馈、图标、字体层级、留白间距、圆角、边框、阴影和颜色对比，统一 hover、focus、disabled、loading、empty、error 等交互状态。设置页按业务域分组并采用渐进披露，减少单页纵向堆叠与横向拥挤，同时提供清晰且固定的保存操作。重设计阶段保留现有业务能力和接口行为，不同时改动后端逻辑。
