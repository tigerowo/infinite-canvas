---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- 补齐 STITCH 桌面、窄屏、状态页与设计 token 实稿；当前仅按 2026-08-24 设计任务书完成代码侧视觉基线，尚无可核对的 STITCH 导出物。
- 为受控 Mac CLI helper 增加签名/哈希版本清单和独立本机伴随进程；当前仅支持显式启用后的固定 `--version` 检测，不执行安装、登录或模型调用。
- 为 RunningHub adapter 补充经用户授权的沙盒账号端到端验证；自动测试只使用本地模拟服务，不调用真实 API。
- 在第三阶段本地提交和 Git 备份完成后，分别使用用户授权的 OpenAI、Gemini 与通用 HTTP 沙盒凭据完成真实契约验证。
- 逐步清理前端 ESLint 兼容基线警告，并视需要增加组件级与浏览器端测试；当前已有正式 lint、typecheck 和 Vitest 脚本。
- 多实例部署前将进程内请求速率和并发预算迁移到共享限流存储；当前单机 SQLite 运行方式无需引入外部依赖。
