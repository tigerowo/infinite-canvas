---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- Antigravity CLI 新增受控 Nano Banana 2 文生图通道：`agy models` 继续只决定可选文本 reasoning model，连接检测仅在当前账号具备固定低成本 `gemini-3.7-flash-low` 时加入虚拟模型 `nano-banana-2`。真实调用固定执行一次内置 `generate_image`，禁用 slash command、启用 sandbox，不接受页面命令、路径或自定义参数，也不会回退到付费 API。后端只在 CLI 正常退出、返回合法 UUID 会话且该会话目录出现本次调用后新建的 32 MiB 内 PNG/JPEG/WebP 时判定成功，再复制到隔离临时目录并按真实 MIME 写入素材存储；不再依赖 `agy 1.1.22` 已变化的 `stream-json` 工具状态字段。连接中心默认文本模型会排除该虚拟工具模型，生图台确认框和结果卡会明确显示 Google 登录态与 `generate_image` 路径。首次真实调用已确认上游成功生成 JPEG，但旧事件判断将其误记为失败；修复后仍需重新安装开发 helper、重启 Go 后端，并由用户逐次确认一张最低成本图片完成闭环。
- 已将原作者 `origin/main` 的 v0.6.0 更新合并到连接中心分支：保留连接中心 Provider、受控 Mac CLI、GPT Image 2/即梦、任务取消与请求预算，同时接入 Agent Skill、检查点长上下文、Chat/Responses 切换、媒体自动生成、内联引用、视频帧截取、流式文本和新版视频节点控件。合并过程未引入 `.env`、SQLite、用户素材或测试产物；合并后的 Go 全包、Vitest 14 个文件 48 个用例、Playwright 10 个用例、typecheck 和生产 build 均通过，lint 为 0 error、118 条既有 warning。回归同时确认即梦受控登录、账户概览及图片模型进入无限画布共享目录，旧测试断言已与现有能力同步。
- GPT Image 2 订阅主通道的检测、非计费预检、最低成本真实文生图、R2 写入、同源素材回显和自动质量长提示词均已完成人工验收，正式能力已移入功能说明。此前已标记为失败的旧卡片不会自动改写；Codex 应急生图仍未执行真实调用，参考图也仍会在启动 CLI 前被拒绝。
- 独立 `Codex 应急生图` CLI Provider 固定使用 `codex exec --model gpt-5.5 --enable image_generation`，不会读取或回退到 OpenAI API Key；生成确认框会提示可能占用 Codex 开发额度。后续如需真实验收，必须再次逐次确认，不得由订阅主通道自动切换。
- GPT Image 2 订阅结果解析定向 Vitest 已通过 1 个文件、4 个用例，覆盖严格 UUID 同源素材地址、危险相对路径和明文 HTTP 拒绝；该用例已纳入本轮全量 Vitest，并与 Go、Playwright、typecheck、lint 和生产 build 一并完成验收。
- Mac CLI helper 的开发安装器和正式安装器会在安装时仅捕获无用户名密码、无查询参数且指向本机回环地址的 `http/https` 代理，并写入 LaunchAgent；开发 helper 重装后已验证 GPT Image 2 订阅请求可以访问上游，远程、带凭据和 SOCKS 代理继续被拒绝。
- Codex 文本最小调用、普通文本节点和 Agent 已固定显式传入 `--model gpt-5.5`；连接中心仍使用虚拟模型 `codex-cli-default`，避免把上游模型名当作用户可编辑参数。

- 生成前确认框已接入画布、生图台、视频台和重试入口，正式请求前显示渠道、模型、任务类型和协议；连接中心用户渠道在云端模式下也应显示真实渠道名称且不暴露凭据。
- 视频、画布图片和画布音频任务对外统一为 `queued`、`running`、`succeeded`、`failed`、`cancelled`、`timed_out`；旧状态读取时兼容归一。
- 三类任务新增显式取消接口；活动视频或图片被删除时会先传播取消。视频陈旧轮询结果不能覆盖已取消状态，画布请求继承可取消上下文。
- 火山方舟 Seedance 的排队视频任务取消会在本地落状态前向官方 `DELETE /contents/generations/tasks/{id}` 传播；RunningHub Provider 新增 `POST /providers/:id/runninghub/tasks/:taskId/cancel` 并调用官方 `/task/openapi/cancel`。两者使用受限响应预算、既有 SSRF 安全客户端与脱敏日志，远端失败不阻断本地取消；其他未公开正式取消协议的供应商不会猜测调用。
- 画布图片、视频和音频运行节点，以及生图台、视频台的运行任务卡新增独立“取消任务”按钮；任务创建阶段按钮暂时禁用，取消后保留记录并显示“已取消”，不再依赖删除记录触发取消。
- 画布图片和音频任务增加 5 分钟总体 deadline，并在代理写入内存时限制 32 MiB 响应；视频任务继续使用 30 分钟、360 次和 32 MiB 累计预算。
- 图片、视频和音频取消服务新增 Vitest 契约覆盖，检查编码后的任务路径、POST 方法、登录鉴权、成功响应和业务失败响应；已纳入 9 个测试文件、29 个用例的全量 Vitest 并通过。
- 用户渠道携带凭据时只允许同源重定向；Base URL 用户信息、fragment、私网解析和自定义 Header CR/LF 注入继续被拒绝。
- 401、403、429 和 5xx 使用固定安全错误映射；普通日志、AI 调用日志和任务错误详情会脱敏 Bearer、API Key、Token、Secret 与常见供应商密钥格式。
- 画布、生图台和视频台的模型选择已统一合并系统公共渠道与连接中心用户 Provider；云端模式下用户 Provider 使用 `X-User-Model-Channel-ID`，系统渠道继续使用 `X-Model-Channel-ID`，退出登录会清除托管目录，未登录旧本地配置仍可继续使用。
- OpenAI 兼容、Gemini 原生与通用 HTTP adapter 已通过本地 Mock 模型列表契约；通用 HTTP 使用原样 Base URL 和标准业务路径，支持 Bearer 或自定义请求头鉴权。
- Mock 失败场景覆盖无效 JSON、鉴权失败、超大响应和请求超时；RunningHub、`gpt-image-2`、OpenOx Gemini 图片兼容模型、Gemini 原生文本模型与通用 HTTP 文本模型均已完成真实任务。
- OpenAI 兼容 Chat 生图现可提取 `choices[].message.content` 中 Markdown 包裹的图片 Data URL；任务响应和错误详情保存前会摘要长 Base64。已用真实 OpenOx 返回结构补充 Go 与 Vitest 回归测试，重启 Go 后端后的 UI 成功态已人工确认。
- 生图工作台结果区增加成功、生成中和失败计数，生成图片按完整比例展示，任务卡强化状态辨识；连接中心概览已按 STITCH v1 收敛为紧凑状态条，避免窄屏占用过多首屏高度。全局信息语义色已从中性主色拆分为 STITCH 青色 token，修复浅色信息 Alert 对比度并保持黑白主按钮不变。
- 生图台浮动工作流按钮的服务端与浏览器首次渲染使用同一初始坐标，客户端恢复已保存位置后再显示，避免位置差异触发 hydration warning 或出现首屏闪跳。
- 生图成功结果中的内联 Data URL 会在写入生成日志前转存到浏览器 localforage，并以 `storageKey` 恢复，同一浏览器刷新后可继续显示且账号历史不写入长 Base64；跨设备或云端长期保留仍需同步到对象存储或保存到素材库。
- 连接中心执行“测试并拉取模型”后，会立即把刷新后的模型列表和默认模型同步回当前编辑表单，不再需要关闭抽屉后重新打开才能选择模型。
- Gemini 原生已使用官方端点完成一次连接测试和一次模型拉取。`gemini-2.5-flash-lite` 的首次请求因上游不再向新用户开放而失败且未自动重试；用户再次确认后，`gemini-3.5-flash-lite` 最低成本文本请求返回 `OK`、`STOP`，真实生成链路通过。
- 通用 HTTP 已使用 OpenOx 完成一次连接测试和一次模型拉取；`gemini-3.5-flash` 的 8-token 请求无可见文本，用户确认后的 64-token 请求遇到一次上游 `EOF`，再次确认后的 128-token 请求返回 `OK`、`stop`。每次请求均独立确认，没有自动重试。
- Provider 只修改默认模型时不再清除最近连接状态；协议、Base URL、密钥或请求头变化仍会重置为 `untested`。修复前已经被重置的本地 Gemini 状态不会伪造恢复，本轮仍不重复测试上游。
- 连接中心 `/providers` 已按 `provider-center-v1` 完成第二轮视觉收敛并通过真实 Provider 数据人工复核：桌面表格固定主要列宽并使用 75px 实测行高，长模型名稳定截断；390px 使用 16px 页面留白和卡片间距、12px 卡片圆角、标签加单行模型信息、右上开关与底部状态区；编辑抽屉保持桌面 440px、窄屏全宽，桌面/窄屏水平内边距分别为 24px/16px，控件高度统一为 40px。深浅色画布、表面、强调、错误、警告和分隔线 token 已集中映射，页面未出现横向溢出。
- 连接中心已使用隔离故障代理复核加载、空、错误、不可用和禁用状态：无缓存数据的加载与错误阶段使用“—”代替误导性的零计数，加载显示 Skeleton，读取失败只显示错误与重试入口，不再叠加空数据引导；中等宽度改用完整卡片，桌面表格从 `lg` 断点开始展示，避免启用和操作列藏在横向滚动中。
- 连接中心编辑抽屉和 CLI 最小调用状态窗已改用 Ant Design 6 的 `size` 与 `mask.closable` API，保持 440px/窄屏全宽及运行中禁止点击遮罩关闭的既有交互，同时消除对应废弃 API 警告。
- 连接中心新增首批 React 组件级状态测试，直接覆盖初始加载 Skeleton 与占位计数、错误和重试互斥空态、API/CLI 空状态操作，以及禁用与不可用标签；前端 Vitest 3.2.4 共 9 个测试文件、29 个用例全部通过，其中连接中心组件状态测试 5 个用例、生图内联结果持久化测试 2 个用例通过。
- 连接中心 Playwright 浏览器 Mock 回归覆盖登录会话持久化、加载转空状态、错误重试恢复、禁用与不可用状态、停用二次确认，以及 390px 卡片和 1280px 表格断点；390px 用例新增页面级横向溢出断言，并通过窄屏导航菜单进入连接中心。所有 `/api/**` 请求均在浏览器侧拦截，不调用真实后端或渠道。
- Playwright 第二批 Mock 回归覆盖 API 抽屉必填校验、协议切换、模型保存、迁移、编辑、复制、删除和跨工作台模型目录。Antigravity 文本模型与即梦图片/视频模型现在都会按能力进入共享目录，生图选择器不会混入文本或视频模型；浏览器用例仍只使用 Mock，不发起真实生成、不修改数据库。
- 请求预算可选接入 Redis 共享存储；跨实例速率和并发计数、主体摘要、租约释放及 Redis 故障安全失败已通过 miniredis 契约测试。另用临时 Redis 7.4 和两个隔离后端实例完成本机运行时演练：第 61 次跨实例请求返回 429，四个并发租约占满后第五次返回 429且释放后恢复，Redis 暂停时约 10 秒安全返回 503并在恢复后重新返回 200。演练未复制用户数据库或请求 Provider，临时资源已清理；未配置 Redis 时继续使用既有单进程预算。
- MiniMax H3 排队任务会调用官方 `DELETE /v2/video_generation/{task_id}`；运行中任务及其他 MiniMax 协议不会误用删除端点，仍只停止本地轮询。
- STITCH v1 已补齐桌面、390px 列表和 390px 编辑表单的正式浅色变体；HTML、PNG、屏幕 ID、生成会话和 SHA-256 元数据保存在参考仓库，现有深色稿与 token 未被覆盖。
- 登录冷启动初始化不再抢跑本地持久化 token；直接访问受保护页面或从登录页站内进入时，会在认证 Store 完成 hydration 后再调用 `/api/auth/me`，避免已有会话被短暂判成未登录。
- 连接中心停用开关新增二次确认并明确保留配置与加密凭据；表单提交失败会定位首个错误。CLI 受控操作区中 Codex 保留检测、状态、登录和最小调用；Antigravity 提供检测并拉取模型、登录状态和固定最小调用；即梦登录状态检查仍只返回已登录/未登录和受控模型。另新增独立账户概览刷新：仅在用户点击时通过受控 helper 返回账号名、会员等级和当前积分，不返回用户 ID、凭据或 CLI 原始输出，也不把账户数据写入数据库。官方 `user_name` 为空时会明确显示“即梦未返回账号名”，不会因此隐藏正常返回的会员等级和积分。积分消费明细区域目前仅为后续功能占位。
- 连接状态使用图标和文字共同表达：“已连接”映射青色、“未测试”映射警告色；连接检测期间列表临时显示带旋转图标的“测试中”，不新增或持久化后端状态。
- 旧 `localChannels` 的只读迁移预览、精确去重、事务导入，以及可选的旧明文密钥清理与渠道 ID 替换；当前账号已在仓库外 SQLite 一致性备份后完成一次真实迁移，导入 1 条 Provider、清理 2 处旧明文凭据，迁移后旧候选和明文凭据均归零且数据库完整性通过。迁移没有测试连接或请求真实 API。
- API Provider 新增、编辑、复制、启停、设为默认、删除及历史引用冲突提示。
- API Key 与自定义请求头留空保持原值，接口和页面不返回明文；显式清空行为待补充 UI 入口后验证。
- 登录用户选择托管 Provider 后，生图、视频、音频和画布请求继续通过 `X-User-Model-Channel-ID` 使用现有调用链。
- 用户 Base URL 访问私网、回环、链路本地、CGNAT 或危险重定向时被拒绝。
- 图片代理拒绝 HTML/SVG 和超过 25 MiB 的响应；AI 代理拒绝 HTML/SVG、超过 128 MiB 的请求和超过 512 MiB 的响应。
- 远端视频和音频下载改用独立 `/api/proxy-media`：继续使用 SSRF 安全客户端、下载请求预算、媒体类型与 HTML/XML 检查，并把单响应限制为 256 MiB；不再误用仅允许图片的 `/api/proxy-image`。本机已用真实即梦临时视频地址确认代理返回 200；当前存储模式为 `local_indexeddb` 且未开放用户对象存储，因此没有继续执行云端文件上传。
- WebDAV 与 S3/R2 容量统计共享每次操作 256 请求、64 MiB、2 分钟预算；WebDAV 使用迭代目录队列，S3 拒绝重复分页标记和无效容量。
- 管理设置的 S3/R2 配置新增月度预算明细：容量、A 类和 B 类分别显示本机计数、额度与进度；默认 80% 预警、90% 写入保护。上传和容量列表分页计入 A 类，后端代理读取计入 B 类；达到容量或 A 类保护线后，新图片和生成媒体回退浏览器本地，已有云端对象仍可读取和删除。该计数明确标注为本机估算，不包含公开域名直连或其他客户端请求，也不替代 Cloudflare 账单。
- 单分类及全量提示词同步限制为 32 请求、32 MiB、2 分钟；全量同步的所有分类共享预算，超限后不替换未完整完成的分类。
- 工作流 Agent 单次响应限制为 8 MiB / 2 分钟；模型列表探测共享 32 请求、32 MiB / 1 分钟并拒绝重复 Gemini 分页标记；后台模型测试单响应限制为 16 MiB。
- KIE/APIMart 同步图片轮询共享 300 请求、32 MiB / 10 分钟；结构化生成响应限制 8 MiB、上传响应限制 512 KiB，MiMo TTS 响应限制 128 MiB。
- 持久化视频任务应在 360 次轮询、32 MiB 累计响应或自创建起 30 分钟任一预算耗尽时失败；重启后 `poll_count` / `response_bytes` 继续累计。Gemini 视频内容与通用 AI 媒体限制为 512 MiB。
- `/api` 的写请求按普通 JSON 2 MiB、同步数据 64 MiB、AI/画布任务 128 MiB、参考媒体 81 MiB、匿名上传 129 MiB 和登录用户对象存储上传 257 MiB 限制；KIE/APIMart 单参考文件超过 32 MiB 时明确失败而不是截断。
- 登录入口按 IP 限制 20 次/分钟、AI 生成按用户限制 60 次/分钟并发 4、上传和重型上游操作限制 30 次/分钟并发 2、下载限制 120 次/分钟并发 4；超限返回 HTTP 429 和 `Retry-After`。
- 每个用户最近 30 分钟最多同时保留 8 个视频、画布图片和画布音频活动任务；并发创建时尚未落库的槽位也参与计算。
- 独立 CLI helper 已使用本机私有 Unix Socket、临时共享密钥和本机签名清单完成 Codex UI 人工回归。Antigravity 已升级为受签名清单约束的模型同步、状态检查、最小调用和无限画布文本入口。即梦官方 `dreamina` 也进入开发安装清单：只读 `user_credit` 仅判断会话可用性；受控目录覆盖图片 3.0、3.1、4.0、4.1、4.5、4.6、4.7、5.0、5.0 Pro，以及 Seedance 2.0、2.0 Fast、2.0 VIP、2.0 Fast VIP、2.0 Mini、2.5。后端按模型限制合法分辨率、视频时长和比例，前端显示友好名称与“待真实验证”，默认使用各模型最低成本参数且不自动调用。页面仍不能传命令、路径或参考素材；任务按用户和 Provider 绑定，输出只接受限额内的结构化状态与 HTTPS 结果地址；取消或超时只停止本机进程和轮询，官方无远端取消命令时上游任务可能继续。
- Next API 代理现使用浏览器可见的原始 `Host` 生成 `X-Forwarded-Host`，不再把 `0.0.0.0` 监听地址误传给后端；后端的回环 Host、Origin 与转发地址检查保持不变。本机 CLI 检测和登录状态已通过该代理路径验证。
- Mac CLI helper 新增正式发布准备链路：离线工具生成 PKCS#8 Ed25519 私钥、公钥和最长 90 天的签名清单；发布脚本构建 universal macOS 二进制、要求 Developer ID Application hardened runtime 签名并核对 Team ID，再从钥匙串配置提交 Apple 公证；安装器再次验证签名、Team ID 与 Gatekeeper 公证评估，使用 `0600` 公钥与共享密钥文件注册当前用户 LaunchAgent，并在启动前执行 `verify-config`。本轮没有 Apple 签名身份，未执行发布、安装或公证命令。
- Mac CLI helper 新增独立本机开发安装模式：只构建当前架构并使用 ad-hoc 签名，临时生成 30 天 Ed25519 清单并绑定当前 Codex SHA-256，使用独立 `cli-helper-dev` 目录、短路径私有 Socket、`.dev` LaunchAgent、随机共享密钥和显式卸载器；正式安装器的 Developer ID、Team ID、Gatekeeper 与公证门槛保持不变。当前 Mac 已完成重复安装、LaunchAgent 运行、`0600` Socket/配置文件权限、后端环境加载和配置签名校验；开发模式仅限个人使用，不可作为发布包分发。
- Antigravity CLI 已安装官方 `agy 1.1.22` 并完成 Google OAuth；helper 清单只接受解析后二进制 SHA-256。无限画布文本助手和普通文本节点通过专用本机 completion 接口启动并轮询。即梦已按图片/视频能力进入生图台、视频台和无限画布的共享模型目录，三处复用同一受控生成接口；首版明确拒绝参考图、首尾帧、参考视频和音频。新增 Go 参数白名单/结构解析测试及前端工作台路由契约；此前 Go `service`、`handler`、`router` 与 Vitest 12 个文件、39 个用例已通过，本次单图结果限制变更另由用户执行 `go test ./service`（通过，约 4.94 秒）及定向 Vitest（1 个文件、3 个用例全部通过）。
- Antigravity `agy models` 在当前 Mac 上约需 8 秒，后端到本机 helper 的受控通信预算由 7 秒调整为 15 秒，避免只读模型同步在 CLI 正常返回前被误判为不可用；真实模型调用仍使用独立的 2 分钟任务 deadline。
- 即梦 CLI 已完成本机安装、登录状态检查和公开模型目录同步准备。早期两次 `jimeng-image-5.0 / 2K / 单图` 调用因 CLI 会话未识别会员权限而在上游任务写入前被拒绝，系统没有自动重试。重新授权后已识别 `maestro` 等级；经用户逐次确认，`jimeng-image-3.0 / 1K / 单图` 与 `jimeng-video-seedance2.5 / 480p / 4 秒` 的最低成本真实调用均成功。单图任务曾因上游返回多个 HTTPS URL 被工作台展开为四条结果；后端现按固定 `--generate_num 1` 契约收敛为首个 URL，前端再做单结果防御，新增的 Go 与 Vitest 回归用例已由用户本地执行通过。其余 13 个模型仍保留“待真实验证”，后续必须逐次确认，不能批量计费调用。
- CLI helper 启用后还必须读取绝对路径的 Ed25519 签名清单；清单签名、过期时间、协议/候选名和解析后二进制 SHA-256 全部通过才执行固定 `--version`。有效、篡改、过期、哈希不匹配和可写文件契约已纳入全量 Go 测试并通过。
- Web 后端的 CLI 检测改为调用独立 `cmd/infinite-canvas-cli-helper` Unix Socket 进程；每次动作授权绑定用户、Provider、协议和动作，使用 30 秒 HMAC、随机 nonce、两分钟防重放缓存和签名响应。有效授权、过期、正文篡改、nonce 重放及私有 Socket 客户端契约已纳入全量 Go 测试并通过。
- CLI Provider 编辑页提供独立“检查登录状态”入口：Codex 固定执行 `codex login status`；Antigravity 通过只读 `agy models` 确认会话并同步模型；即梦登录检测通过只读 `dreamina user_credit` 确认会话但丢弃账户字段。即梦编辑抽屉另提供逐次触发的“账户概览”刷新，只提取账号名、会员等级和当前积分三项展示字段；用户 ID、认证材料和原始响应不会返回页面，即梦 OAuth 仍由用户直接在官方 CLI 中完成。
- Codex CLI Provider 新增“登录 Codex”二次确认入口；接口还要求 `confirmed=true`，确认后逐次授权启动固定无参数 `codex login` 浏览器 OAuth。登录全局互斥、最长 10 分钟、stdin 为空、输出全部丢弃，helper 退出时取消；API Key、Access Token、device code、自定义参数、Gemini/即梦登录均未开放。本轮只补确认门槛、动作绑定、固定参数、互斥和接口路径契约测试，未实际触发登录。
- Codex CLI Provider 新增“最小调用”预览与二次确认：固定使用当前默认模型、固定提示词、只读沙箱、临时目录和 2 分钟 deadline，不接受自定义参数且不自动重试。任务 ID 与用户、Provider、协议绑定，状态为 `running`、`succeeded`、`failed`、`cancelled` 或 `timed_out`，最终输出脱敏并限制为 4 KiB；运行中提供独立取消按钮。确认窗、固定参数、任务越权、逐次授权和前端 start/status/cancel 契约已通过人工回归、Go 与 Vitest；当前 OAuth 会话已通过本机开发 helper 完成 `连接中心 → Go 后端 → 私有 Socket → Codex CLI` 的单次真实最小调用，状态为 `succeeded` 且最终输出严格为 `OK`，未自动重试。
- Codex CLI 已进入共享文本模型目录，使用虚拟模型名 `codex-cli-default` 表示当前登录账号的 CLI 默认模型；无限画布普通文本节点和 Agent 现在都可通过连接中心选择该渠道。画布提示词仅经 stdin 传给固定 `codex exec`，继续使用临时目录、只读沙箱、两分钟 deadline、4 KiB 输出上限、全局单任务与取消传播，不接受命令、工作目录或额外参数。Go 固定参数/提示词隔离测试及普通文本节点、Agent 的 Vitest 路由契约已补充，仍需用户逐次确认一次真实画布调用。
- CLI Provider 普通保存不再把已连接状态重置为“不可用”；只有协议、Base URL、凭据发生变化或渠道从禁用恢复时才要求重新检测，并清除旧检测时间。`检测 CLI → 保存 → 返回列表` 已完成人工复核并继续显示“已连接”。
- RunningHub 应用/工作流提交、查询和累计读取预算已通过本地模拟服务测试；真实账号的一次账户检测与一次工作流任务已成功，任务状态为 `QUEUED → RUNNING → SUCCESS` 并返回 1 个 PNG。
- Linux.do OAuth state 应带签名、时效并与 HttpOnly SameSite Cookie 匹配；回调 URL 只携带两分钟有效且只能使用一次的交换码，不再出现 JWT。
