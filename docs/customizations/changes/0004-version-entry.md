# EXT-0004：隐藏顶部版本号入口

## 目标与范围

上游基线：`51c4503b2c53e237b2fe67c65711b51ae5deb762`。隐藏用户标注的顶部 `v0.6.0` 版本与更新说明入口；沿用公共导航配置，在公共导航及画布的同一入口组件中生效。版本数据、更新说明组件和后端保持原样。

## 扩展配置及上游接入

在 `web/src/extensions/navigation/config.ts` 增加 `showVersionEntry: false`，改为 `true` 可恢复入口。无环境变量、样式或数据库变化。

| 上游文件与符号 | 改动 | 必须修改的原因 | 同步检查点 |
| --- | --- | --- | --- |
| `web/src/components/layout/user-status-actions.tsx` / `UserStatusActions` | 版本组件渲染增加扩展配置判断 | 上游直接渲染版本组件，需在渲染位置接入显示配置 | 配置、主题按钮保留，版本入口继续受扩展配置控制 |

## 验证

状态：页面验证通过。浏览器 DOM 确认顶部已无版本按钮，配置与主题切换入口仍在，GitHub 和登录入口保持隐藏；四个轮播指示点仍在。`git diff --check` 通过，代码差异仅为新增配置字段和一处条件渲染。本项为显示开关调整，未重复执行此前已报告既有错误的全量类型检查或生产构建。定制进度集中记录于本文件，上游 TODO 和待测试文档无需重复添加。

## 提交与回退

本项独立提交：`feat(ext-navigation): 隐藏顶部版本号入口`。恢复 `showVersionEntry` 即可重新显示；完整回退时撤销这一配置字段及渲染判断。不涉及数据恢复。启动自动生成的 `web/next-env.d.ts` 不纳入本项。
