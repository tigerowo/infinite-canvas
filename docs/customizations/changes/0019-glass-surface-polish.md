# EXT-0019：玻璃表面层级收口

## 实际修改

- 移动导航抽屉只让外层承担玻璃背景和模糊，标题栏与主体改为透明，避免三层实色背景叠加后失去层次。
- 提示词卡片接入统一玻璃表面 token，保留原有边框、悬停和焦点反馈。

## 验证

- TypeScript 检查通过。
- Next.js 生产构建通过，20 个页面生成成功。
- `git diff --check` 通过。
- 浏览器在 375px 和 1280px 视口检查首页无横向溢出，控制台错误数为 0。
